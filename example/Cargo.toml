[package]
name = "example"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
config-checker = { path = "../config-checker", version = "*" }
serde_derive = "^1.0"
serde = { version = "^1.0", features = ["derive"] }

[dependencies.confy]
version = "^0.5"
features = ["yaml_conf"]
default-features = false

